class Inventory:
    def __init__(self):
        """
        To-do:
        - Implement inventory creation functionality
        - Implement inventory modification functionality
        - Implement inventory retrieval functionality
        """

    def create_inventory(self):
        """
        To-do:
        - Prompt user for inventory items (e.g. weapons, armor, potions, etc.)
        - Validate user inputs to ensure they comply with game rules and restrictions
        - Store inventory items in a data structure (e.g. list, dictionary, etc.)
        - Return the inventory object
        """

    def modify_inventory(self):
        """
        To-do:
        - Prompt user for the inventory items they wish to modify
        - Validate user inputs to ensure they comply with game rules and restrictions
        - Update the inventory items based on the modified information
        - Return the modified inventory object
        """

    def get_inventory(self):
        """
        To-do:
        - Prompt user for the inventory items they wish to retrieve
        - Return the requested inventory items
        """

    def _validate_inputs(self, inputs):
        """
        To-do:
        - Implement validation logic for inventory inputs
        - Return True if inputs are valid, False otherwise
        """

    def _get_item_info(self, item_name):
        """
        To-do:
        - Use D&D 5e API to retrieve information about the specified item
        - Parse the API response and return relevant information (e.g. description, damage, weight, etc.)
        """
